// Main export for AIInsightsTab component
export { AIInsightsTab } from './AIInsightsTab'

// Re-export types for consumers who need them
export type {
    AIInsightsTabProps,
    ViewState,
    OptimizationObjective,
    OptimizerPersistedState,
    CanvasState
} from './types'

